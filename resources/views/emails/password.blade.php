<!DOCTYPE html>
<html>
<head>
    <title>{{ __('passwords.subject') }}</title>
</head>
<body>
    <h1>{{ __('passwords.reset') }}</h1>
    <p>{{ __('passwords.action') }}</p>
    <a href="{{ $url }}">{{ __('passwords.reset_link') }}</a>
    <p>{{ __('passwords.thank_you') }}</p>
</body>
</html>
